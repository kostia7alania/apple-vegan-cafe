# Backlog

Протокол: команда **«работай»** = взять верхнюю задачу из READY и довести до
Definition of Done (CI зелёный, задеплоено, отмечено здесь). Задачи из BLOCKED
не трогать — они ждут внешнего действия. Порядок в READY = приоритет.

## READY (могу делать автономно)

Автономных задач, которые можно честно закрыть без новых business facts, не осталось.
Следующий проход начинается после ответов из `MVP-FACTS-LOCK.md`.

## BLOCKED (ждут тебя или владельца кафе)

Сначала заполнить короткий [MVP facts lock](./MVP-FACTS-LOCK.md). Пока ответ не
подтверждён владельцем, не публиковать догадки из отзывов или каталогов как факты кафе.

| #   | Задача                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Ждёт                     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| B1  | ~~Реальное меню~~ **сделано 2026-07-18, обновлено 2026-08-14** (144 блюда из owner Grab-экспорта, EN/TH/RU). `SITE_LAUNCHED=true` включен 2026-07-21, сайт открыт для индексации; GSC domain property подтверждён и sitemap принят; остаток: хозяйка проверяет меню по пути                                                                                                                                                                                                                                                | проверка хозяйкой        |
| B2  | GitHub-доступ хозяйки в `/admin`: отправить персональный email-инвайт коллаборатора через Repository Settings → Collaborators → Add people. Хозяйка принимает инвайт, выбирает «Continue with Google» и нейтральный username; коммиты публичны, инвайт живёт 7 дней. Затем проверить вход по HANDOVER-th §1.                                                                                                                                                                                                               | отправка инвайта (Костя) |
| B4  | Google Business Profile: доступ/verification, категория Vegan restaurant, меню-ссылка на сайт, review-QR; точный geo-пин + Maps URL → в locations.json                                                                                                                                                                                                                                                                                                                                                                     | доступ владельца         |
| B5  | ~~Фото из текущего Grab-каталога → в карточки блюд~~ **сделано 2026-08-14**: 144/144 exact ItemID, responsive 300px + detail до 1000px, bytes не лежат в Git. Отдельная пересъёмка нужна позже только для hero/интерьера и замены слабых старых dish assets.                                                                                                                                                                                                                                                               | hero/пересъёмка позже    |
| B6  | HappyCow claim (тикет) + создать TripAdvisor-листинг                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | владелец                 |
| B7  | ~~Google Search Console~~ **подтверждён 2026-08-19**: verified domain property; sitemap Success, 31 discovered pages. Остаток: UptimeRobot keyword-монитор + Bing Places + Apple Business Connect. Для monitor уже готов read-only handoff: `infrastructure/uptime-monitor.json`, `pnpm monitor:check` и runbook; внешний monitor и alert contact по-прежнему не настроены.                                                                                                                                                | твои аккаунты            |
| B8  | Спросить хозяйку: спец-условия с Grab; есть ли halal-сертификат (если да — точные issuer/scope/reference), используется ли алкоголь в готовке и какой owner-approved комментарий публиковать; какие реальные блюда содержат морковь (для 🪷-тега); подтвердить, что картофеля/имбиря нет вообще; согласие называть веру семьи на сайте; **в Grab два одинаковых «Спагетти с грибным соусом» (฿199, THITE…09904 и THITE…95416) — удалить дубль в GrabMerchant?**; цены у стойки vs цены Grab (что печатать в бумажном меню) | владелец                 |
| B9  | ~~Cloudflare Web Analytics~~ **включена 2026-07-24 через dashboard edge injection**. Не добавлять `PUBLIC_CLOUDFLARE_WEB_ANALYTICS_TOKEN` в repo Variables — это задвоит beacon. GA4 нужен только для Ads/UTM. `pnpm funnel:report -- <export.csv>` готов, но baseline нельзя считать без полного 30-дневного aggregate export.                                                                                                                                                                                            | 30 дней данных           |
| B10 | **HTTP→HTTPS ещё не настроен:** 2026-08-19 apex и глубокие EN/TH/RU URL возвращали полный `200` по HTTP. В Cloudflare включить zone-level **Always Use HTTPS** и проверить один постоянный redirect с сохранением path/query; не добавлять Worker к assets-only deploy. Затем настроить www→apex через proxied DNS + zone Redirect Rule. HSTS preload отложить до стабильной работы обоих redirect и всех нужных subdomain; текущий HSTS без `preload`. Точный DoD — в runbook.                                            | доступ к Cloudflare zone |
| B11 | Подтвердить реально активные delivery platforms: оставить GrabFood, добавить LINE MAN с официальным share/order URL или явно подтвердить, что LINE MAN не используется. До ответа production остаётся Grab-only, а пустой/неизвестный провайдер не публикуется.                                                                                                                                                                                                                                                            | владелец                 |

## LATER (по плану, не сейчас)

- SEO schema spike: отражать `Restaurant & Cafe` дополнительным schema.org-типом. Текущий безопасный `Restaurant` уже работает; карта, фото и business facts дадут MVP больше пользы
- Языки phase 2 (zh-Hans, ko, de) — только с носителем-редактором
- Catering-страница — когда услуга реальна
- Онлайн-заказ на сайте (корзина → LINE/телефон; Workers Paid $5/мес при SSR)
- WebMCP — пока не внедрять: сайт read-only и не имеет действия, которого нет в обычных ссылках `tel:`/delivery; добавление протокола не даёт MVP-пользы. Пересмотреть, если появится реальный онлайн-заказ на сайте.
- Тайский UI-локаль для Sveltia CMS — законтрибьютить upstream (в бандле 0.172.4 только en/ja — проверено грепом тайских/японских строк). Пока компенсировано: все лейблы/опции нашего config.yml переведены на тайский (2026-07-30), непереводимым остаётся только хром Sveltia (Save, Sign in, поиск). После мержа upstream — поднять пин версии
- Приватный модуль: рецептуры/граммовки/себестоимость/чек-листы (отдельное приложение Workers+D1+Access, отдельный private-репо)
- Мультифилиальность (locations[] уже готова)

## DONE

- 2026-08-20: **Menu import совместим с защищённым `main` и доказан end-to-end** — dry-run job оставлен read-only, а `contents/pull-requests: write` выдаются только manual apply. Bot действительно создал confirmation PR; GitHub удержал оба PR-workflow как `action_required`; после maintainer approval `CI` проверил `refs/pull/20/merge`, полный `verify` прошёл и ruleset стал `CLEAN`, без deploy. Первый proof перед этим поймал реальный formatting churn в importer: исправлено на byte-preserving замену только `category`, `price_thb`, `available`, invariant проверен на 144 блюдах. Оба proof PR и временные ветки удалены без мержа тестовой цены.
- 2026-08-20: **Production deploy теперь gated полным CI и проверенным artifact** — Cloudflare workflow больше не запускается параллельно каждому push и не имеет отдельного manual bypass. `CI` один раз собирает `dist` с production Variables, завершает lint/typecheck/content/build/link/E2E/Lighthouse, сохраняет immutable artifact + SHA-256 и только затем передаёт тот же artifact reusable deploy на конкретном main SHA. PR никогда не деплоится, failed/cancelled verify, отсутствующий Cloudflare secret или ручной rerun устаревшего SHA дают красный release, а новый push отменяет старый run до позднего релиза. Новый reusable workflow имеет отдельный identity; legacy `Deploy` ID `314276139` выведен из эксплуатации удалением старого path (GitHub state `deleted`), а его ещё-rerunnable history удалена, чтобы старое определение нельзя было запустить повторно.
- 2026-08-19: **Thai Jay landing вернулся как fact-only 200-page** — Search Console показал, что `/th/ร้านอาหารเจ-พัทยา/` уже индексировался и получил 22 показа/1 клик, но review-gate отправлял этого посетителя 302-редиректом в общее меню. Непроверенный семьёй cultural/festival long-form не опубликован: вместо него route использует только подтверждённые источники сайта — 100% plant-based/เจ, актуальные часы, адрес, телефон и четыре data-driven блюда с текущими фото/ценами. URL снова self-canonical, имеет только `hreflang=th`, включён в sitemap и связан с TH home; семейная вычитка старого лонгрида не заявляется.
- 2026-08-19: **Menu decision-first redesign** — первый мобильный экран теперь ведёт к еде, а не к длинной стопке пояснений: компактный EN/TH/RU search стоит рядом с H1, четыре demand-informed позиции показаны крупным food-first блоком `Popular on Grab`, restaurant-wide vegan/jay truth свернута в native details, а price policy оставлена видимой тихой заметкой. Удалены бесполезный фильтр до ฿200 (он оставлял 93% меню), повторный jay badge на каждой карточке и постоянная кнопка «наверх» из category rail; с 1024px карточки идут в две колонки. Полный каталог, цены и Menu JSON-LD остаются server-rendered; progressive bundle ограничен CI-контрактом до 6 KiB и не становится условием индексации.
- 2026-07-30: **Админка по-тайски для хозяйки.** Все лейблы, подсказки и опции селектов в `public/admin/config.yml` переведены (тайский первым, английский в скобках — чтобы Костя мог подсказывать по телефону): коллекции, поля блюд, категории (названия 1:1 с categories.json), теги (1:1 с ui.ts), аллергены, дни недели, источник данных. Сводка блюда в списке теперь «тайское имя · EN — ฿цена». Добавлены hint-подсказки к «ยังขายอยู่ (Available)», slug, Draft, Translation key. HANDOVER-th синхронизирован с новыми лейблами + §1 переписан под персональный аккаунт через email-инвайт (B2). Сам интерфейс Sveltia — только en/ja (см. LATER про upstream-контрибуцию; `CMS.registerLocale` — официальный стаб «not to be implemented», рантаймом локаль не добавить). Для разработчика добавлен английский твин **`/admin/en`** (тот же бекенд/контент, лейблы английские, свой config.yml через `rel=cms-config-url`); структурное равенство двух конфигов охраняет `scripts/check-admin-config-parity.ts` в validate:content/CI. Проверено на проде: оба конфига отдаются, /admin/en тянет свой config (200), экраны логина без ошибок.
- 2026-07-24: **Pagefind добавлен и удалён в тот же день.** На production он не работал из-за CSP/WASM, поэтому зависимость и глобальный поиск убрали. Позже, 2026-08-10, появился узкий dependency-free progressive search только внутри меню; старое обещание «0 JS везде» больше не является контрактом.
- 2026-07-24: **R31** — ADR-0005 и `Restaurant.additionalType = CafeOrCoffeeShop` завершены upstream. Первая версия `public/llms.txt` позже признана небезопасной по неподтверждённым business facts и пересмотрена вместе с августовскими truth gates.
- 2026-07-24 (смена «20 тасок»): **N1–N20 выполнены на том снимке.** Price guard, print menu, a11y, link audit, cache и CMS updates сохранены; новые тогда FAQ/landing claims про late-night delivery/pickup позднее удалены или переведены на fail-closed owner facts.
- 2026-08-14: **Grab live-menu + media resync** — свежие owner CSV/API подтвердили 144 ItemID и три активные позиции, отсутствовавшие на сайте: Vegan spring rolls, Mushroom naem и Vegan Luuk Chuey добавлены по exact ItemID без выдуманных New/Popular. Все 144 Grab catalogue images опубликованы через responsive remote URLs (card + detail до 1000px) без image bytes в Git. Provider metadata содержит 143 Vegan и ошибочный Vegetarian у Crispy breaded vegan chicken; публичный UI больше не даёт этому внешнему полю переопределять подтверждённые кафе vegan/jay факты. Остальные подписи/персонажи остаются частью самих catalogue images. Все 144 service-type item prices внутри Grab совпадают, поэтому сайт подтверждает совпадение website↔Grab, но не обещает цену у стойки.
- 2026-08-11: **Grab photo recovery connector + personal skill** — `pnpm grab:photos` принимает owner-supplied Grab catalogue JSON/Partner API menu payload/sanitized HAR, сопоставляет фото только через стабильный ItemID map (144 после resync 2026-08-14), в discovery показывает точные hosts без signed query, а в write-mode требует explicit host allowlist и атомарный private staging. Downloader проверяет DNS/redirect SSRF, Content-Type+magic, ≤2 MiB/4000px, SHA-256 и не трогает dish JSON/public. Публичный media sync теперь хранит нейтральный provenance `grab-merchant-catalogue`, exact ItemID и snapshot date.
- 2026-08-11: **External identity research** — каталоги дали конфликтующие pin и часы, поэтому непроверенные Place ID/координаты сознательно не сохранены в publishable repo. Round 1 не якорит владельца на чужих данных: просит открыть реальный Google Maps pin, нажать Share и вставить owner-generated URL. Google review URL должен быть скопирован из того же verified GBP; TripAdvisor и LINE MAN остаются owner-confirmation-only.
- 2026-08-11: **Thai-first owner Round 1** — `pnpm intake:round1` печатает в stdout короткое LINE-ready сообщение ровно с 25 answer lines на том же контракте, что и полный Facts Lock: Maps/address, hours, messaging, payment, pickup/price, delivery, reservation status и scoped final permission. Неподтверждённые ответы всегда `unknown`/unchecked; текущие address/hours/Grab/primary-contact показаны только в отбрасываемых `#` comments и должны быть подтверждены или исправлены. Numeric geo candidate и пустые `not set` подсказки удалены после product-аудита. Unknown round-trip даёт 0 errors/0 mappings; заполненный fixture — 0 errors и 12 точных mappings без записи content.
- 2026-08-10: **Contact FAQ CTA truth fix** — ссылка на ответы для визита теперь зависит от подтверждённых payment methods: при пустом production-факте EN/TH/RU обещают только реально доступные ответы об allergens и delivery и ведут в общий FAQ; при подтверждённой оплате label добавляет payment и ведёт прямо к `#payment`. UX-аудит после правки не нашёл P0/P1.
- 2026-08-10: **30-day funnel report handoff** — добавлен read-only `pnpm funnel:report -- <export.csv>` с privacy-safe контрактом ровно из `date,page_path,event_name,provider,count`. Команда требует 30 последовательных завершённых дней Asia/Bangkok и явное покрытие `page_view`/`order_click`/`phone_click`/`directions_click`/`review_click` по каждому date × locale × observed provider, поэтому пропущенный сегмент выводит `INSUFFICIENT DATA`, а не ложные 0%. Валидный export даёт deterministic totals/rates по EN/TH/RU и provider плюс три opportunity prompts с явной evidence-базой. MVP-119/120/123 остаются BLOCKED до production analytics decision и реального export.
- 2026-08-10: **draft + attribution truth gates** — непроверенный семьёй TH เจ landing больше не публикует long-form copy: route отдаёт static 302 на проверенное `/th/menu/`, получает `noindex` и исключён из sitemap единым gate. Все восемь статей используют нейтрального автора-организацию `Apple Vegan Cafe & Restaurant`; CMS больше не подставляет семью автоматически, а Article JSON-LD отражает тип `Organization`. Явные claims «написано/проверено семьёй» удалены, family-run позиционирование сохранено.
- 2026-08-10: **FAQ certification/fasting truth cleanup** — EN/TH/RU больше не утверждают неподтверждённое отсутствие gluten-free сертификата и не обещают пригодность для православного/религиозного поста «по определению». Публичный ответ ограничен подтверждённым plant-based составом, предупреждением о traces и советом сверить индивидуальные правила про масло/алкоголь/строгие дни.
- 2026-08-10: **pickup negative-state invariant** — `pickup: null` остаётся unknown; explicit `enabled: false` запрещает contact/lead time/price note, а `enabled: true` требует реально доступный phone/LINE/WhatsApp path. Инвариант совпадает в Astro schema, standalone validator, owner-intake checker и Thai handover; production остаётся `null`, MVP-080–082 не закрыты без ответа владельца.
- 2026-08-10: **read-only owner-intake checker** — `pnpm intake:check -- <filled-facts-lock.md-or-txt>` детерминированно отделяет validation errors, подтверждённые значения и BLOCKED placeholders, проверяет HTTPS/provider URLs, даты, enum и атомарные зависимости maps/messaging/delivery/payment/price/pickup/reservations/spice/dietary/halal/media/final permission. График поддерживает единый диапазон или непересекающиеся day groups, адрес/payment/pickup требуют полные EN/TH/RU группы, а повторяемые dish blocks проверяют реальный slug, allergen registry и verifier/date перед точным `foodFacts.*` mapping. До полного FINAL разрешения mappings не выдаются; скрипт ничего не записывает, неизвестное не превращает в publishable claim. Ledger statuses не менялись.
- 2026-08-10: **MVP-083 multi-provider readiness** — `orderingLinks` теперь принимает ровно GrabFood/LINE MAN, отклоняет дубли и небезопасные/чужие LINE MAN URL, а Home/Menu/Contact/mobile/FAQ × EN/TH/RU условно показывают все подтверждённые ссылки. При двух платформах mobile bar сохраняет максимум четыре действия и ведёт к выбору на Menu. Production остаётся только с текущим GrabFood URL; сам MVP-083 остаётся BLOCKED до подтверждения владельцем активных платформ и официального LINE MAN link либо явного отказа от него.
- 2026-08-10: **site-wide food-safety claim scrub** — устранён дрейф за пределами FAQ: Thai เจ landing, EN/RU breakfast и три опубликованные статьи больше не обещают zero cross-contact/«ничего не нужно проверять» и не доказывают безопасность отсутствием мяса в помещении. Fish-sauce FAQ также больше не заявляет абсурдно узкий полный состав «только soy sauce, salt, herbs». Все поверхности сохраняют подтверждённое plant-based меню и направляют severe-allergy гостей уточнить процесс до заказа.
- 2026-08-10: **cross-contact/Jain landing truth cleanup** — EN pure-veg landing больше не обещает zero cross-contact, отсутствие garlic/onion/root vegetables, Jain-friendly блюда или веру семьи до проверки. Title/hero/cross-link теперь честно позиционируют подтверждённые plant-based เจ facts и явно сообщают, что Jain recipe details проверяются по блюдам. EN/RU breakfast также перестали обещать, что «ничего не нужно перепроверять» и еда не готовится рядом с мясом; severe-allergy guidance синхронизирован с FAQ.
- 2026-08-10: **owner-reviewed dish-description gate** — единый `getPublicDishDescription()` закрывает все public consumers: неподтверждённый editorial description не попадает в menu/home/landing UI, client search index и Menu JSON-LD, пока `reviewedAt` пуст. При этом names/prices из официального owner Grab export остаются публичны; importer и отдельный foodFacts verifier не менялись. CMS/HANDOVER прямо предупреждают, что owner review date открывает description, но не подтверждает allergens/остроту/Jain/GF. После resync 2026-08-14 production остаётся 144/144 `reviewedAt: null`; MVP-042/043/055 остаются BLOCKED до реального owner review.
- 2026-08-10: **About privacy/truth cleanup** — из публичного About × EN/TH/RU удалены неподтверждённые детали «пара + мама + бывший шеф», которые противоречили BLOCKED MVP-071. Подтверждённое family-run позиционирование сохранено; имена, роли и личная история появятся только после согласования формулировок и границ приватности семьёй.
- 2026-08-10: **Search Console verification hook** — `PUBLIC_GOOGLE_SITE_VERIFICATION` условно добавляет verification meta через общий SEO head и пробрасывается в production build из GitHub Variables; при пустом значении тег отсутствует. Runbook даёт точный handoff до submit `sitemap-index.xml` и честно оставляет GSC в состоянии «not configured». MVP-107 остаётся BLOCKED до внешней верификации и принятого sitemap.

- 2026-08-10: **halal-guidance truth gate** — прежний FAQ больше не утверждает неподтверждённые отсутствие сертификата, отсутствие animal-product cross-pan и комфорт Muslim guests. Добавлен единый nullable `halalGuidance`: certification status + cooking alcohol + owner-approved note + verifier/date обязательны вместе; `certified` дополнительно требует локализованные issuer/scope/reference details, а `not-certified` запрещает их. Schema/CMS/content validator используют Bangkok non-future date, FAQ × EN/TH/RU при `null` честно сообщает, что сертификат и cooking alcohol не подтверждены, а verified state перечисляет факты без обещания «halal-safe». Production остаётся `null`; MVP-069 остаётся BLOCKED до полного ответа владельца.
- 2026-08-10: **spice-request truth gate** — базовая острота блюда больше не может быть принята за обещание кастомизации. Добавлен отдельный nullable owner-policy `not-offered | offered` с точными request levels, обязательным scope × EN/TH/RU и verifier/date. Menu + FAQ показывают подтверждённую политику только при заполненной валидной записи; production `null` ничего не обещает. MVP-089 остаётся BLOCKED до ответа кухни, но после него не потребуется новый кодовый проход.
- 2026-08-10: **deterministic local-listing packet** — добавлен `pnpm generate:listing-packet`, который без сети собирает copy-ready NAP, regular hours и website/menu/order/review URLs для Google Business Profile, HappyCow, Tripadvisor, Bing Places и Apple Business Connect из текущих repo sources. `null` Maps/profile/review/geo остаются буквальным `NOT CONFIRMED — DO NOT PUBLISH`; generated Markdown содержит source paths и не содержит timestamp, поэтому повторный запуск не шумит diff. Внешние outcomes MVP-108–115 остаются BLOCKED до owner access, pin и фактической синхронизации профилей.
- 2026-08-10: **featured truth-label cleanup** — публичный `featured` title на EN/TH/RU теперь означает нейтральную подборку без popularity/bestseller claim; CMS честно предупреждает, что флаг управляет home и menu quick picks. Отдельный неподтверждённый `Popular for delivery` также заменён нейтральным заголовком. Текущие четыре `featured` блюда не менялись; MVP-039 остаётся BLOCKED до выбора 8–12 позиций владельцем или по Grab sales.
- 2026-08-10: **unverified customization/oat-price copy cleanup** — adversarial TH review removed two unconditional promises to adapt dishes when only ingredient questions are confirmed; strict Jay/diet guests now get the same ask-before-ordering instruction. The same pass removed three stale TH article prices (฿150/120/119 vs live ฿189/149/189), all remaining hardcoded prose prices, and unverified “house-made oat milk” claims across EN/RU breakfast/articles. Dish prices remain sourced from menu data; oat/plant milk can still be stated without claiming an unconfirmed production process. MVP-070 remains BLOCKED until the family confirms recipe and coverage.
- 2026-08-10: **ingredient-question contact gate** — добавлен отдельный `dietaryQuestionsContact`, который нельзя автоматически наследовать от общего primary contact: владелец явно подтверждает phone/LINE/WhatsApp только если команда реально отвечает на вопросы о составе. При валидной связке Menu и FAQ × EN/TH/RU показывают channel-specific CTA с отдельным analytics event и предупреждением, что объяснение ингредиентов не гарантирует отсутствие аллергенов; при `null` или отсутствующем messenger URL CTA полностью исчезает. Временный phone fixture подтвердил обе menu placements + cross-contact FAQ и удалён; реальные allergen outcomes остаются BLOCKED до owner workflow/facts.
- 2026-08-10: **price-policy truth gate + reservations plumbing** — устранена коллизия, где breakfast без подтверждения обещал counter prices, а delivery одновременно называл те же значения Grab prices. `operations.pricePolicy` теперь единственный nullable источник (`same_everywhere`, counter, Grab или channels differ); при production `null` EN/RU лендинги используют нейтральное «проверьте итоговую цену в выбранном канале». Для reservations добавлена fail-closed CMS/schema модель: unknown скрыт, подтверждённый отказ показывается честно, accepted/large-groups требуют lead time, рабочий канал и для групп — minimum size; Contact и FAQ синхронно рендерят policy × EN/TH/RU, а CTA только просит бронь и не обещает подтверждение. Временный large-group/phone fixture прошёл все 10 целевых surfaces и удалён; MVP-057/082/085 остаются BLOCKED до owner values.
- 2026-08-10: **fail-closed food facts pipeline** — все блюда используют явную tri-state модель `foodFacts`: unknown отдельно от подтверждённых остроты 0–3, contains-allergens, «без ингредиентов с глютеном» и Jain recipe. Непроверенные факты не рендерятся, не индексируются поиском и не создают фильтры; verified-only filters появляются только при реальном покрытии, неизвестный diet URL даёт 0 verified matches и явный reset, а Quick Picks следуют активным фильтрам. CMS/schema/content gate требуют verifier/date и запрещают выдавать пустой allergen list за allergen-free; importer сбрасывает facts и скрывает новую/изменённую строку до owner review. После resync 2026-08-14 production остаётся 144/144 unknown. Outcomes MVP-047/065–068 остаются BLOCKED до проверки рецептур семьёй.
- 2026-08-10: **rights-cleared media pipeline** — добавлен отдельный CMS/schema registry для hero, exterior, family и interior, а старый bare-path `dish.images[]` заменён тем же approved asset object. Для каждого файла обязательны размеры, provenance, правообладатель, scope, дата/автор подтверждения и явная проверка узнаваемых людей; licensed media требует публичный credit. Поскольку CMS кладёт файлы в `public/uploads`, pending/unknown/denied assets запрещены до загрузки и ломают content gate вместо тихой публикации; CSV importer больше не принимает голый `photo_file` и сохраняет уже одобренные dish images при resync. Разрешённые изображения автоматически заменяют иллюстративный hero, появляются на Home/Contact/About/menu и в deduplicated Restaurant JSON-LD; empty state не оставляет дыр, а временный hero явно помечен как иллюстрация. Временный собственный SVG-fixture подтвердил четыре site slots × EN/TH/RU и был удалён; реальные outcome MVP-022–025/040/072 остаются BLOCKED до оригиналов и разрешений владельца.
- 2026-08-10: **hours single-source sweep** — `locations.hours` стал единственным literal графика во всём `src`: home SEO, FAQ, breakfast/delivery/Jay landings, About и статьи больше не дрейфуют после CMS-save. Добавлен общий formatter; исправлен Thai `{close}` placeholder и удалён старый Mon–Sat/฿60–150 фрагмент. Special-hours UI теперь заранее показывает исключения на ближайшие 7 дней по Bangkok date, но скрывает regular hours только в сам день. Headless fixture подтвердил: +1 и +7 видны, +8 скрыт, regular hours остаются видимыми.
- 2026-08-10: **owner-facts plumbing sweep** — добавлен fail-closed путь CMS → schema → EN/TH/RU UI для LINE/WhatsApp/preferred contact/response hours, Google profile vs direct review, landmark/parking/arrival note, payment methods и pickup. Footer умеет показывать несколько атрибутированных профилей, mobile bar сохраняет максимум четыре действия, Maps попадает в Restaurant `hasMap`, analytics различает profile/review clicks. Временный заполненный fixture подтвердил автоматическое появление значений на home/contact/FAQ ×3 и в JSON-LD; production values возвращены в `null`. FAQ больше не утверждает неподтверждённые cash и отсутствие cross-contact. Итоговые owner outcomes остаются BLOCKED до фактов, но новых кодовых правок после заполнения формы не потребуется.
- 2026-08-10: **MVP choose/operations sweep** — в полном меню появились quick picks из существующего `featured`, пояснение растительных названий, совместимые search + price filters до ฿150/฿200 и синхронизация выбранного состояния с URL. Исправлены 32 очевидные механические ошибки в `name.en`; полный RU scan не нашёл доказуемых механических дефектов. `specialHours` из CMS теперь заменяют обычные часы в текущую дату Паттайи на home/contact/footer и попадают в Restaurant schema. Неподтверждённый pickup удалён из публичного copy, а зона/статус доставки делегированы live-данным GrabFood. READY исчерпан: ledger фиксирует 66 DONE и 57 задач, заблокированных реальными данными/доступами владельца.
- 2026-08-10: **MVP conversion sweep** — добавлен локальный поиск по всем доступным блюдам на EN/TH/RU без внешнего поискового сервиса; неподтверждённое обещание «GrabFood delivery 24/7» заменено честным «проверьте актуальный статус в GrabFood», а gluten-free copy больше не обещает несуществующие проверенные метки; из About ×3 локали убраны публичные draft/placeholder-фразы, добавлены ссылки на меню и контакты, а GrabFood и самовывоз описаны без новых обещаний. Для недостающих business facts создан `docs/MVP-FACTS-LOCK.md`.
- 2026-07-22: **R30 SEO outbound link context pass** — проверены повторяющиеся GrabFood/HappyCow/Instagram ссылки в сгенерированном HTML: GrabFood оставлен как conversion CTA, Instagram остаётся единственным footer social-link, HappyCow оставлен как endorsed trust/review ссылка без `nofollow`; убран шумный fallback, который дублировал тот же HappyCow URL вторым footer CTA «Leave us a review» на каждой странице. Добавлен Playwright guard на один contextual HappyCow footer link.
- 2026-07-22: **R29 SEO link graph audit** — добавлен `pnpm audit:links` (`scripts/audit-links.ts`): проверяет внутренние HTML-ссылки и якоря из `dist`, корректно декодирует Thai URL, игнорирует assets/service pages и отличает broken links от low-inbound warnings. Скрипт сразу поймал и исправил реальный RU anchor bug: `/ru/menu/#one-plate` → `/ru/menu/#osnovnye-blyuda`. Текущий результат: audit проходит, остаются два warning по low-inbound blog articles.
- 2026-07-22: **R28 SEO metadata trim pass** — укорочены слишком длинные title/H1/description на EN/RU статьях и landing pages без потери основного intent: delivery и pure-veg H1 теперь не дублируют бренд, EN home description короче, RU/EN blog titles чище. После build локальный audit по порогам `title > 60`, `description > 155`, `h1 > 65` возвращает `flagged=0`.
- 2026-07-22: **R27 SEO internal linking pass** — добавлены контекстные внутренние ссылки без искусственных «кольцевых» блоков: EN/RU welcome и how-to статьи ведут к breakfast/delivery, EN Jay статья ведёт к pure-veg/Jain странице, RU home получила breakfast/delivery entry points, RU breakfast/delivery лендинги связаны по сценарию «утро vs доставка», TH welcome ведёт к Thai Jay landing. После build целевые входы: EN breakfast 6, EN delivery 5, pure-veg 3, RU breakfast 6, RU delivery 6, TH Jay 3.
- 2026-07-22: **R26 SEO-quality page metadata guard** — `pnpm validate:content` теперь отклоняет небезопасный Markdown-frontmatter у страниц: пустые/обрезаемые `title`/`description`, неверный `locale`, несоответствие locale filename-суффиксу и неполные translation-set по локалям, чтобы структурные страницы не теряли SEO-метаданные или hreflang-пары.
- 2026-07-22: **R25 SEO-quality article date guard** — `pnpm validate:content` теперь отклоняет небезопасные даты статей в Markdown-frontmatter: отсутствующий/невалидный `publishedAt`, несуществующие календарные даты, будущие публикации и `updatedAt` раньше `publishedAt`, чтобы сортировка блога и Article JSON-LD не дрейфовали.
- 2026-07-22: **R24 SEO-quality location geo guard** — `pnpm validate:content` теперь отклоняет небезопасные координаты в `locations.json`: отсутствующие/нечисловые lat/lng, значения вне диапазонов latitude/longitude и placeholder `0,0`, чтобы будущий Google pin не ломал JSON-LD/Local SEO.
- 2026-07-22: **R23 SEO-quality published article discoverability guard** — Playwright теперь автоматически собирает published-статьи из Markdown frontmatter и проверяет, что каждый URL есть в локальном blog index и `sitemap-0.xml`, а draft-URL не попадают в индексы.
- 2026-07-22: **R22 SEO-quality draft article exposure guard** — Playwright теперь автоматически собирает draft-статьи из Markdown frontmatter и проверяет, что их URL отдают 404 и не попадают в `sitemap-0.xml`, чтобы черновики не утекали в публичную индексацию.
- 2026-07-22: **R21 SEO-quality article slug guard** — `pnpm validate:content` теперь отклоняет небезопасные article URL slugs (`/`, `?`, `#`, `%`, пробелы, пустые/обрезаемые значения и dot segments вроде `.`/`..`), чтобы Markdown-статьи не порождали проблемные `/blog/{slug}/` URL.
- 2026-07-22: **R20 SEO-quality category slug guard** — `pnpm validate:content` теперь отклоняет небезопасные category anchors (`/`, `?`, `#`, пробелы, пустые/обрезаемые значения), дубли category slug по локалям и конфликты category/dish anchors, чтобы меню не получало дублирующиеся DOM `id`.
- 2026-07-22: **R19 SEO-quality business external link guard** — `pnpm validate:content` now rejects unsafe configured social/order/review/maps URLs before deploy: invalid URLs, non-HTTPS links, embedded credentials, unsupported providers/platforms, and unexpected hosts outside the cafe’s business-link allowlists.
- 2026-07-22: **R18 SEO-quality dish slug guard** — `pnpm validate:content` теперь отклоняет небезопасные dish anchors (`/`, `?`, `#`, пробелы, пустые/обрезаемые значения), дубли текущих dish slug по локалям, конфликты `previousSlugs` с живыми slug и дубли старых slug между блюдами.
- 2026-07-22: **R17 SEO-quality redirect/content guard** — `pnpm validate:content` теперь отклоняет небезопасные redirect-записи до деплоя: неверные status code, дубли source, self-redirect, external/protocol-relative URL, query/hash URL и service paths вроде `/admin/` или `/uploads/`.
- 2026-07-22: **R16 SEO-quality structured data guard** — Playwright парсит JSON-LD на ключевых страницах, проверяет ожидаемые безопасные schema.org-типы (`Restaurant`, `BreadcrumbList`, `Menu`, `Article`), absolute URL в `url`/`menu`/`item`, `OrderAction`, полную Menu-разметку на 141 доступное блюдо с THB Offer и отсутствие `aggregateRating`/`review`/`FAQPage`.
- 2026-07-22: **R15 SEO-quality manifest/head discoverability guard** — Playwright проверяет базовые head discovery-теги (`theme-color`, favicon, manifest, sitemap), валидность `site.webmanifest` (name/short_name/display/colors/icons) и доступность `favicon.svg`.
- 2026-07-22: **R14 SEO-quality sitemap coverage guard** — Playwright парсит `sitemap-0.xml`, проверяет канонические public URL для основных EN/TH/RU страниц, уникальность/trailing slash/absolute loc и отсутствие служебных `/admin/`, `/uploads/`, query/hash URL; LHCI запускает 3 прогона, чтобы performance-budget не падал от холодного первого запуска на GitHub runner.
- 2026-07-22: **R13 SEO-quality canonical/hreflang matrix** — Playwright проверяет self-canonical и точный набор head `hreflang`/`x-default` для структурных страниц, парных лендингов, одноязычных страниц и переведённых статей.
- 2026-07-22: **R12 SEO-quality social preview guard** — Playwright проверяет, что `/`, `/menu/`, `/th/menu/`, `/ru/menu/` отдают absolute `og:url`, `og:image`, размеры 1200×630 и `twitter:card=summary_large_image`; `/og-default.png` доступен как PNG.
- 2026-07-21: **R11 SEO-регрессия индексации** — Playwright guard проверяет, что `/`, `/menu/`, `/th/menu/`, `/ru/menu/` не получают `meta robots noindex`, `robots.txt` не закрывает корень и указывает на sitemap, а `sitemap-index.xml` генерируется с `sitemap-0.xml`.
- 2026-07-21: **Индексация + deploy + analytics foundation** — `SITE_LAUNCHED=true`, live больше не отдает `noindex`, sitemap/robots доступны; `CLOUDFLARE_API_TOKEN` добавлен в GitHub и Deploy workflow реально деплоит через Wrangler; добавлена опциональная аналитика: Cloudflare Web Analytics и GA4 через repo Variables, GA4-события `phone_click`/`order_click`/`review_click`/`directions_click`/`outbound_click`, CSP обновлен, без переменных live не грузит third-party scripts.
- 2026-07-20 (ночь, 10 задач): **e2e-регрессии фактов** (часы ежедневно, 141-блюдная паритетность с Grab, CTA, OrderAction — 23 теста); **JSON-LD Menu/MenuSection/MenuItem** ×3 локали; **тайские имена** 12 западных блюд; **RU-лендинги** `/ru/veganskaya-dostavka-v-pattaye/` и `/ru/veganskiy-zavtrak-v-pattaye/` со взаимным hreflang (+фикс несвежих «young coconuts» в EN); **UX меню** (счётчики в чипах категорий, back-to-top); **perf-аудит** (меню 9КБ br на проводе, TTFB ~0.6с — фиксы не нужны); **описания 5 сигнатурных блюд** ×3 локали; **доки** (HANDOVER-th: таблица 5 категорий + балк-ресинк; README: quarterly re-sync); **аудит**: 477 внутренних ссылок целы, security-заголовки на месте. R10: www не имеет DNS-записи — нужна ручная запись + Redirect Rule (инструкция в отчёте)
- 2026-07-18 (ночь): **B1 реальное меню на проде** — 141 блюдо из официального Grab Bulk-Update-экспорта хозяйки (5 настоящих категорий, цены ฿90–439), 43 тайских названия переведены на EN, все 141 названы по-русски вручную; Grab-ссылка в CMS → кнопки «Order on GrabFood» на главной/контактах/доставке и (2 CTA) на странице меню ×3 локали; джайн-лендинг ограничен 10 блюдами; вычищены захардкоженные «Sunday — Closed» и «Mon–Sat» на трёх лендингах; `scripts/data/grab-item-map.json` (ItemID→файл) + `docs/grab-resync.md` для квартальных ре-синков; пайплайн зелёный, задеплоено
- 2026-07-17: **языковой тогл** — глобус + CSS-only `<details>`-дропдаун (ноль JS, эндонимы, текущий язык с галочкой, fallback на главную локали, crawlable-ссылки в DOM) — готов к 10+ языкам; **культурные бейджи** 🌱/🪷/🌾 в меню, jain-friendly тег в схеме и CMS, FAQ про пост и халяль (без сертификатных заявлений); часы 7:00–22:00 ежедневно по всему сайту, JSON-LD полная неделя
- 2026-07-17 (ночь): **R9 HANDOVER-th.md** — полная тайская инструкция для семьи (8 секций, точные подписи кнопок из config.yml, PWA-установка на телефон, 6 плейсхолдеров под скриншоты — добавить при передаче); **quality-sweep**: код-ревью ночного диффа (без замечаний: Seo og-блок аккуратный, conditional Grab корректен, дублей meta нет) + Lighthouse 5 новых страниц (perf/a11y зелёные, SEO-балл ограничен намеренным pre-launch noindex)
- 2026-07-17 (ночь): **R6 FAQ** `/faq/` ×3 локали (7 Q&A: gluten/cross-contact с честным unknown-state, доставка, часы и conditional payment facts; БЕЗ FAQPage-разметки) + пункт в навигации; **R7** site.webmanifest + og-default.png (20KB, sharp-генерация из брендовых SVG) + og:image/twitter-мета на всех страницах
- 2026-07-17 (ночь): **R5 статьи блога** — EN «Vegan vs เจ» + «How to order vegan in Thailand», RU «Как заказать веганскую еду» + «Веган-гид по Паттайе» (вычитаны мной, опубликованы), TH «เจ กับ วีแกน» (draft до семьи); все факты захеджированы, цены только из коллекции
- 2026-07-16 (ночь): **R3+R4 EN-лендинги** `/vegan-delivery-pattaya/` (Grab-условный CTA — апгрейдится сам, когда семья добавит ссылку в CMS; честная заметка про цены приложений) и `/pure-veg-jain-friendly/` (честный Jain-блок без обещания полного Jain-меню); кросс-линк блок «More for you» на всех трёх EN-лендингах; fix: compressHTML съедал пробелы у <strong>
- 2026-07-16 (ночь): **R2 EN breakfast-лендинг** `/vegan-breakfast-pattaya/` — угол «двери с 7:30», 3 завтрака + 2 утренних напитка с ценами, банер с EN-главной
- 2026-07-16 (ночная смена, историческое состояние): **R1 TH เจ-лендинг draft** `/th/ร้านอาหารเจ-พัทยา/` — long-form текст, фестиваль и рекомендации ждали вычитки семьёй; тогда gate отдавал static 302 на `/th/menu/` и исключал URL из sitemap. С 2026-08-19 это состояние superseded компактной fact-only 200-page из DONE выше; непроверенный long-form по-прежнему не публикуется.

- 2026-07-16: **R8 дизайн «Thai garden table»** — крем/зелень/манго + เจ-бейдж, Playfair (self-hosted latin+cyr), компактная мобильная шапка (111px), CSS-only нижний бар (меню/звонок), dotted-leader меню с бейджами и 🌶, внешняя оценка дизайна: PASS (3/3 design, 3/3 originality) + refine-раунд. Попутно исправлен прод-баг: все блюда схлопывались в одну запись (glob loader брал localized slug-объект как id) — на живом меню было одно блюдо

- 2026-07-15: research (8 агентов) + архитектурное решение, scaffold: Astro 7 SSG, EN/TH/RU, Zod-коллекции, CI (lint/typecheck/vitest/playwright/lighthouse/links), security headers, Sveltia CMS, Renovate
- 2026-07-16: домен apple-vegan-cafe.com (Cloudflare Registrar) + деплой на Workers Static Assets, репо public, auth-Worker на auth.apple-vegan-cafe.com (юзер поставил OAuth-секреты, логин работает), placeholder-меню 14 блюд ×3 языка, noindex-гейт, legal menu-import workflow (diff→PR), fix: двойной CSP на /admin, шрифты Sveltia, deploy.yml

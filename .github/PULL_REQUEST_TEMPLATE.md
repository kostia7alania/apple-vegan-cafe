## What & why

<!-- One or two sentences. Link the BACKLOG item or issue if there is one. -->

## Checklist

- [ ] `pnpm lint && pnpm check && pnpm test && pnpm validate:content` pass
- [ ] `pnpm build && pnpm audit:links && pnpm test:e2e` pass
- [ ] No invented facts: prices/hours/claims trace to the owner, the Grab
      export, or existing content (see CONTRIBUTING «House rules»)
- [ ] Zero client-side JS on content pages is preserved
- [ ] Visual change → screenshots attached (mobile 390px + desktop)
- [ ] Thai copy (if any) is marked `draft: true` pending family review
